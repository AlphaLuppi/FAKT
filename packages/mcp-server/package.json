{
  "name": "@fakt/mcp-server",
  "version": "0.1.11",
  "private": true,
  "type": "module",
  "description": "Serveur MCP (stdio) qui expose les API de FAKT comme outils à Claude Code CLI.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "start": "bun run src/index.ts",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@fakt/shared": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.19.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6",
    "@types/node": "^22.9.0",
    "@types/bun": "^1.1.0"
  }
}
