{
  "name": "@fakt/ai",
  "version": "0.1.0",
  "description": "AI provider interface + Claude CLI subprocess wrapper for FAKT",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "peerDependencies": {
    "@fakt/shared": "workspace:*"
  },
  "peerDependenciesMeta": {
    "@fakt/shared": {
      "optional": true
    }
  }
}
