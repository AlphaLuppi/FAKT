{
  "name": "@fakt/pdf",
  "version": "0.1.3",
  "private": true,
  "description": "PDF renderer — Typst templates + Tauri bridge for FAKT quotes and invoices.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6",
    "vitest": "^2.1",
    "@vitest/coverage-v8": "^2.1",
    "@fakt/core": "workspace:*",
    "@fakt/legal": "workspace:*",
    "@fakt/shared": "workspace:*"
  }
}
